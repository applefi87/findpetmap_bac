const articleImage = {
  maxAmount: 4,
  maxFileSize: 5
};
const normalImage = {
  quality: 80,
  width: 1080,
  height: 1080
};
const previewImage = {
  quality: 60,
  width: 80,
  height: 80
}

export default { articleImage, normalImage, previewImage };